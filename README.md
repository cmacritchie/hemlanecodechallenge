Code Challenge for Hemlane

install dependencies with:
`npm install`

run application:
`npm run start`

This code should be run with the backend code found here: https://github.com/cmacritchie/hemlanebackend